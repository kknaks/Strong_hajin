# 업무(Task)의 정의

업무(Task)는 **담당자가 기대하는 결과와 완료 기준을 정하고, 그 결과를 만들기 위한 작업·요청·진행 상태를 관리하는 단위**다. 업무를 만드는 행위는 본인 업무 생성, 업무 요청, 업무 분배(배정)로 구분한다.

업무는 하나의 결과를 중심으로 관리한다. 결과는 문서나 디자인 파일뿐 아니라 회의록, 상태 변경, 검토 의견처럼 수행 사실과 내용을 확인할 수 있는 기록일 수도 있다.

- **담당자**: 업무를 수행하고 진행 상태와 결과를 기록하는 사람이다.
- **기대 결과와 완료 기준**: 무엇을 만들어 내거나 바꾸어야 하며, 어떤 결과를 확인하면 끝났다고 판단하는지 정한다.
- **수행 항목**: 필요한 경우 직접 수행할 하위 작업과 다른 사람에게 보낼 요청으로 나눈다.

업무는 하위 작업 없이도 존재할 수 있다. 예를 들어 회의를 진행하고 회의록을 업데이트하는 것만으로 완료 기준을 충족한다면, 하나의 업무로 관리하면 된다.

## 1. 업무를 만드는 세 가지 행위

### 1.1 본인 업무 생성

내가 직접 수행할 업무를 등록하는 행위다. 예를 들어 내가 작성할 보고서를 내 업무로 만드는 경우다.

### 1.2 업무 요청

다른 사람에게 일을 요청하고, 수락·협의를 거쳐 결과를 확인하는 행위다. 예를 들어 디자이너에게 표지 디자인을 요청하는 경우다.

### 1.3 업무 분배(배정)

배정 권한을 가진 사람이 업무를 수행할 담당자를 지정하는 행위다. 예를 들어 팀장이 권한 범위 안에서 부서원에게 업무를 배정하는 경우다.

### 1.4 구분 원칙

- ‘업무 생성’은 넓게 보면 요청·분배로 업무가 생기는 경우도 포함하므로, 첫 번째 행위는 **본인 업무 생성**이라고 부른다.
- 업무 요청과 업무 분배는 모두 상대방을 지정하지만, 서로 다른 기능이다. 담당자 유무로 구분하지 않는다.
- 메일·슬랙·AI 채팅 등 유입 경로와 위 세 행위의 구분은 별개다.
- 각 행위의 생성 시점, 수락·거절, 완료 확인 규칙은 구분해서 정리한다. 한 흐름의 규칙을 다른 흐름에 그대로 적용하지 않는다.

## 재검토할 기존 초안: 업무의 기본 구조

> 아래 구조와 DB 변경 방향은 이전 논의 기록이다. 업무 요청과 업무 배정을 혼동한 설명이 포함되어 있어 확정 명세나 구현 지침으로 사용하지 않는다. 특히 요청을 `tasks`와 `task_assignments`로 통합한다는 기존 제안은 철회했다. 위 세 행위의 구분을 기준으로 각 항목을 다시 검토한다.

### 1. 업무 본체와 하위 작업

각 담당자는 자신이 맡은 업무를 중심으로 바로 아래에 수행 항목을 관리한다. 하위 작업에는 자신이 직접 수행하는 작업과 다른 사람에게 요청한 작업이 함께 들어갈 수 있다.

```text
나의 업무: 보고서 발행
├─ 표지 디자인 요청 — 디자이너에게 요청 보냄
└─ 보고서 내용 작성 — 내가 직접 수행
```

이 문서에서는 결과 전체를 관리하는 단위를 **업무**, 그 안의 수행 항목을 **하위 작업**이라고 부른다. **하위 작업도 각각 독립된 Task이며, 담당자·진행 상태·결과·완료 기준을 가진다.** 직접 수행하거나 다른 사람에게 요청하여 수행할 수 있다.

하위 작업 사이에는 기본적으로 수행 순서가 없다. 내용 작성과 디자인은 병렬로 진행할 수 있다. 목록의 배치 순서만으로 선행 작업이 끝나야 다음 작업을 시작할 수 있다는 제약을 만들지 않는다.

### 2. 하나의 Task와 재귀적인 구성 관계

**요청자와 수신자는 동일한 Task를 본다.** ‘표지 디자인’을 요청자용과 디자이너용으로 각각 만들지 않는다. 수락 여부는 이 Task의 담당 배정에 기록하고, 수행 상태와 결과는 해당 Task에 기록한다.

업무는 상위 업무 없이 독립적으로 요청할 수도 있고, 기존 업무의 하위 업무로 요청할 수도 있다. 하위 업무이면 `parent_task_id`로 바로 위 업무를 가리킨다. 요청받은 사람도 자신이 맡은 Task 아래에 직접 수행하거나 다른 사람에게 요청할 하위 Task를 만들 수 있다.

```text
전체 데이터 구조
보고서 발행 — 나
├─ 표지 디자인 — 디자이너
│  ├─ 레퍼런스 조사 — 디자이너
│  ├─ 시안 구성 — 디자이너
│  ├─ 인사 정보 확인 — 인사팀
│  │  └─ 필요한 추가 작업 — 인사팀
│  └─ 초안 검토 — 나
└─ 보고서 내용 작성 — 나
```

**전체 데이터는 재귀 구조이고, 각 담당자의 기본 화면은 자신이 맡은 업무와 바로 아래 하위 업무 한 단계만 보여준다.**

| 업무를 여는 사람 | 중심 업무 | 기본으로 표시하는 하위 업무 |
|---|---|---|
| 나 | 보고서 발행 | 표지 디자인, 보고서 내용 작성 |
| 디자이너 | 표지 디자인 | 레퍼런스 조사, 시안 구성, 인사 정보 확인, 초안 검토 |
| 인사팀 담당자 | 인사 정보 확인 | 필요한 추가 작업 |
| 나 | 초안 검토 | 검토를 위해 직접 추가한 하위 업무가 있으면 표시 |

‘표지 디자인’은 보고서 발행의 하위 업무이면서, 디자이너가 자신의 작업을 관리하는 중심 업무다. 누가 화면을 열었는지에 따라 `parent_task_id`가 바뀌지는 않는다. 초안 검토를 내가 맡아도 그 상위 업무는 표지 디자인으로 유지한다.

내 보고서 발행 화면에는 디자이너의 세부 작업을 기본으로 펼치지 않는다. 표지 디자인의 진행 상태와 결과를 보고, 더 자세한 내용이 궁금할 때 해당 Task로 들어가 접근 권한 범위 안에서 바로 아래 작업을 확인한다. 기본 화면은 한 단계만 표시한다. 생성 규칙에서도 같은 담당자가 직접 작업을 하위의 하위로 계속 분해하는 것은 막는다. 다른 담당자가 요청을 수락한 업무를 중심으로 작업을 추가할 때 전체 데이터의 깊이가 늘어날 수 있다.

### 3. 상태와 결과의 작성 책임

요청 수락 전에는 담당 배정의 수락 대기 여부를 표시한다. 수락 후에는 수신자가 같은 Task에 진행 상태와 결과를 기록하고, 요청자는 그 값을 조회한다. 요청자용 상태를 별도로 복제하거나 중복 입력하지 않는다.

수락과 실제 시작은 구분한다. 수락하면 수행 책임이 확정되며, 시작 버튼을 누르거나 진행 중으로 변경할 때 수행을 시작한 것으로 기록한다. 요청 시각·수락 시각·최초 시작 시각을 각각 남긴다.

| 상황 | 요청자에게 보이는 의미 | 기록 주체 |
|---|---|---|
| 요청을 보내고 응답을 기다림 | 수락 대기 | 요청을 보낸 기록에 따라 표시 |
| 수신자가 수락했지만 시작 전 | 수락됨 / 시작 전 (`open`) | 수신자 |
| 수신자가 시작함 | 진행 중 (`in_progress`) | 수신자 |
| 진행이 막힘 | 차단 상태와 사유 | 수신자 |
| 수행 결과를 남김 | 제출된 결과 또는 완료 | 수신자 |
| 요청자의 승인이 필요한 경우 | 확인 대기 후 승인 또는 보완 결정 | 요청자 |

위 표현은 의미를 설명한 것이며 DB 상태값을 확정한 목록은 아니다. 요청의 수락 상태, 수행 업무의 진행 상태, 제출 결과에 대한 승인 여부는 구분해서 관리한다.

승인이 필요 없는 요청은 수신자의 완료를 요청자에게 반영한다. 승인이 필요한 요청은 결과 제출과 승인을 구분하고, 합의한 완료 기준에 따라 완료를 반영한다. 모든 요청에 승인을 필수로 요구하지는 않는다.

### 4. 검토와 보완의 반복

검토도 담당자와 결과가 있는 업무로 요청할 수 있다. 검토 업무의 결과는 승인일 수도 있고 보완 의견일 수도 있다. **보완 의견을 작성해 전달했다면 검토 업무는 완료할 수 있지만, 검토 대상인 디자인 업무까지 완료된 것은 아니다.**

```text
디자이너의 업무: 표지 디자인
├─ 레퍼런스 조사 — 완료
├─ 시안 구성 — 완료
├─ 1차 검토 요청 — 완료 / 결과: 보완 필요
├─ 시안 수정 — 완료
└─ 2차 검토 요청 — 완료 / 결과: 승인
```

1. 디자이너가 나에게 1차 검토를 요청한다.
2. 나는 이를 수락해 ‘표지 디자인 검토’ 업무를 수행하고, 보완 내용을 결과로 남겨 완료한다.
3. 디자이너의 1차 검토 요청에 완료와 검토 결과가 반영된다.
4. 디자이너는 자신의 표지 디자인 업무에 ‘시안 수정’과 ‘2차 검토 요청’을 추가한다.
5. 나는 새 검토 요청을 수락하고, 수정안을 확인한 뒤 승인 결과를 남겨 검토 업무를 완료한다.
6. 디자이너가 표지 디자인의 완료 기준을 확인하고 업무를 완료한다.
7. 내 보고서 발행 안의 표지 디자인 요청에도 완료와 최종 결과가 반영된다.
8. 나는 내용 작성 등 필요한 작업과 보고서 발행의 완료 기준을 확인하고 업무를 완료한다.

각 검토 요청에는 검토할 결과물의 버전을 연결한다. 이전 검토의 결과를 새 검토로 덮어쓰지 않는다. 위 예시의 1차 검토·시안 수정·2차 검토는 표지 디자인 아래의 동급 Task로 추가한다. 검토 담당자가 자신의 검토 업무를 다시 세분화하면 그 아래에 하위 Task가 생길 수 있다. 깊이는 실제 구성 관계에 따라 늘어나며, 보완 의견에 응답했다는 이유만으로 수정 업무를 이전 검토 아래에 붙이지 않는다.

### 체크리스트와 하위 업무

체크리스트는 업무의 완료 여부를 판단하는 확인 항목이고, 하위 업무는 그 기준을 충족하기 위해 수행하는 Task다. 체크리스트는 생성 단계부터 작성할 수 있으며, 모든 활성 항목을 확인해야 업무를 완료할 수 있다. 전체 체크가 업무의 자동 완료를 뜻하지는 않는다. 현재 구현에는 미완료 체크리스트에 대한 완료 차단 검증을 추가해야 한다.

### 5. 구조의 원칙

- 하위 작업도 각각 담당자·진행 상태·결과·완료 기준을 가진 Task다.
- 하나의 요청 대상 업무는 하나의 Task로 관리한다. 수락 시 수신자용 Task를 추가로 복제하지 않는다.
- 각 Task는 바로 위 업무를 `parent_task_id`로 가리키며, 상위 업무가 없으면 값을 비운다.
- 같은 담당자의 직접 작업 분해는 중심 업무 아래 한 단계로 제한한다. 다른 담당자가 요청을 수락한 Task는 그 수신자의 중심 업무가 되어 하위 Task를 가질 수 있다. 따라서 전체 데이터는 재귀 구조다.
- 각 담당자의 기본 화면은 중심 업무와 직속 하위 업무 한 단계만 표시한다.
- 담당자가 달라도 구성 관계는 유지된다. 요청·수락은 담당 배정으로, 수행 상태와 결과는 동일한 Task로 관리한다.
- 하위 업무가 모두 완료됐다는 이유만으로 업무 본체를 자동 완료하지 않는다. 담당자는 본체의 기대 결과와 완료 기준을 확인한다.

### 부록. 현재 DB 구조와 변경 방향

> 아래는 합의한 모델을 구현하기 위한 변경 방향이다. 현재 코드에 모두 구현되어 있다는 뜻은 아니며, 이 문서 수정으로 DB나 코드를 변경하지 않는다.

#### 1. 기존 Task와 담당 배정 구조 사용

`tasks.parent_task_id`는 이미 `tasks.id`를 참조한다. 단독 업무는 값을 비우고, 하위 업무는 상위 Task의 ID를 저장한다. 모든 하위 작업을 Task로 관리하므로 별도 `task_work_items` 테이블은 제안하지 않는다. 기존 체크리스트를 하위 Task와 동일한 개념으로 취급하지 않는다.

`task_assignments`에는 `task_id`, `assignee_id`, `status`, `accepted_at` 등이 있다. 이 구조를 이용해 동일한 Task에 대한 요청 대상과 수락 여부를 관리한다. 요청자가 누구인지, 요청 발송 시각과 거절 시각을 어떻게 남길지도 일관되게 정한다.

#### 2. 요청 생성 경로 정리

현재 코드는 두 경로가 다르다.

- 일반 업무 요청: `work_requests` 생성 후 수락 시 Task 생성.
- 담당자 지정: Task와 수락 대기인 `task_assignments`를 먼저 생성.

이 문서의 모델에서는 요청 발송 시 대상 Task가 존재하고, 수락 시 같은 Task의 담당 배정을 활성화하는 방향으로 정리한다. 일반 요청 경로도 이 모델로 통합하거나 연결 방식을 변경해야 한다. `work_requests`의 유지·통합·전환 여부와 요청 내용·협의·검토 이력 및 기존 데이터의 처리 방식은 구현 단계에서 결정한다. 현재 문서에서는 테이블 삭제나 기존 기능 제거를 확정하지 않는다. 기존 수락 코드가 추가 Task를 생성하지 않도록 함께 변경해야 한다.

#### 3. 재귀 구조 지원

DB의 자기 참조 컬럼은 여러 단계의 관계를 표현할 수 있지만, 현재 코드는 하위 업무 아래의 하위 업무 생성을 일괄 금지한다. 다른 담당자가 수락한 요청 업무는 자신의 하위 작업을 가질 수 있도록 제한을 변경하고, 같은 담당자의 직접 작업 분해는 한 단계로 유지한다. 관련 조회·권한·완료 검증도 함께 수정해야 한다.

- 생성과 상위 업무 변경 시 자신 또는 자신의 후손을 상위로 지정하는 순환을 방지한다. 동시 변경에서도 순환이 생기지 않도록 처리한다.
- 다른 담당자가 요청을 수락한 하위 업무에서도 직속 하위 업무를 조회하고 생성할 수 있게 한다.
- 기본 화면은 직속 하위 업무만 조회하고, 상세 이동으로 다음 단계를 확인한다.
- 요청 관계만으로 상위·하위의 모든 자료 접근 권한을 자동 부여하지 않는다. 상세 조회마다 권한을 확인한다.
- 현재 완료 검증과 진행 집계가 한 단계만 전제하는지 점검한다. 상위 완료, 하위 취소·재개, 집계 범위의 정책을 정해 여러 깊이에서도 일관되게 적용한다.

#### 4. 상태·시간·검토 결과

- 요청 수락 상태와 Task의 수행 상태는 분리한다. 수락 후 시작 전에는 `open`, 시작 시 `in_progress`로 전환한다.
- 요청·수락·최초 시작 시각을 각각 기록한다. 시작 시각은 기존 상태 변경 이력에서 최초 `open → in_progress` 이벤트의 `occurred_at`으로 확인한다. 별도 `started_at` 컬럼은 추가하지 않는다. `start_date`는 날짜 값으로 구분하고, 재개로 최초 시작 기록을 덮어쓰지 않는다.
- 요청자와 수신자는 같은 Task의 상태와 결과를 조회한다.
- 검토 Task의 `done`과 검토 결과인 승인·보완 필요를 구분한다.
- 검토 요청마다 대상 결과물과 버전을 연결하고, 기존 `submissions`, `review_assignments`, `review_decisions`와 중복된 결정 기록이 생기지 않도록 연결 방식을 정한다.

생성 및 요청·수락·시작의 상세 흐름은 [업무의 생명주기](lifecycle.md)에서 논의한다.

=======================위로는 정리본이 올라옴======================
업무가 생성되는 케이스 ?   
1. 본인이 직접 업무 생성 
2. 남이 요청 해서 생성 
3. 메신저에서 오는 업무 생성 
4. 회의에서 생성되는 업무 
5. ai 채팅으로 생성하는 업무 

업무의 정의 
1. 정의 : 조직에서 일하는 최소 단위로 업무들이 쌓여서 프로젝트/행정 등 과 같이 사업이 진행된다. 
2. 특징
- 
- 항상 작업결과가 파일/상태업데이트/로그 등으로 존재 ("000에게 전달하기"와 같은 것은 업무가 아니다.)
- 시작일과 종료일이 존재한다. 
- 로그를 남길 수 잇어야 한다. 


업무의 성격 
1. 신규 생성
- 정의 : 연관된 작업없이 처음 생성된 업무 
- 예시 : 보고서 작성하기, 코드 작성하기 

2. 연관 업무 
- 정의 : 연관된 작업 (선행과 후행이 명확)하여 다음 업무로 생성된 업무 
- 

3. 하위 업무 
- 정의 : 상위 업무를 위해서는 반드시 수행 되어야 하는 업무, 상위 작업 시 
- 

업무의 진행 과정 

? 진행 중인 업무는 어떻게 추적 할것인가 : 
