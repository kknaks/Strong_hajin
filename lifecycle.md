# 업무의 생명주기

## 개요

업무의 생애주기는 **생성 → 진행 → 결과 제출·확인 → 완료**를 기본 흐름으로 본다. 확인이 필요 없는 업무는 수행 후 바로 완료할 수 있다. 수행 중에는 진행이 막히거나 업무가 취소될 수 있고, 완료 후 다시 진행할 수도 있다.

현재 코드의 `tasks.state` 상태값을 기준으로 표현하면 다음과 같다. 생성 경로와 요청 수락은 이 상태 흐름에 진입하는 과정이며, 별도로 정의한다.

```text
생성 → open → in_progress → done
                  │
                  ├─ blocked → in_progress
                  │
                  └─ completion_submitted
                         ├─ 승인 → done
                         └─ 보완 요청 → in_progress

done → 재개 → in_progress
진행 중인 업무 → 허용된 취소 전이에 따라 cancelled
```

| 상태값 | 의미 |
|---|---|
| `open` | 업무가 생성되었고 수행 시작 전인 상태 |
| `in_progress` | 수행 중 |
| `blocked` | 진행이 막혀 사유를 남긴 상태 |
| `completion_submitted` | 완료 보고를 제출하고 확인을 기다리는 상태 |
| `done` | 완료 |
| `cancelled` | 취소 |

위 상태값은 현재 구현을 설명하는 기준이다. `open`만으로 담당자가 수락했다고 판단하지 않는다. 현재 담당자 지정 방식에서는 Task가 먼저 생성되고 배정은 수락 대기일 수 있다. 요청·배정의 수락 여부, Task의 진행 상태, 결과에 대한 검토 결정은 각각 구분한다.

또한 검토 Task 자체의 `done`은 검토를 끝냈다는 뜻이다. 검토 결과가 승인인지 보완 필요인지는 별도 결과이며, 검토 대상 업무의 완료와 같지 않다.

## 1. 업무 생성

> 이 장은 definition.md에서 합의한 목표 모델을 설명한다. 현재 일반 요청 코드의 ‘수락 시 Task 생성’ 방식은 이 모델에 맞게 변경해야 한다.

### 1.1 생성 정의와 특징

업무 생성은 수행할 일과 기대 결과·완료 기준을 정해 하나의 Task로 등록하는 것이다. 생성, 요청 수락, 실제 시작은 서로 다른 사건이다. 생성 시 Task의 상태는 `open`이며, 이 상태만으로 수락 여부를 판단하지 않는다.

업무는 독립적으로 만들거나 기존 Task의 하위 업무로 만들 수 있다. 독립 업무는 `parent_task_id`를 비우고, 하위 업무는 바로 위 Task의 ID를 저장한다. 생성자가 직접 수행할 수도 있고 다른 사람에게 수행을 요청할 수도 있다.

### 1.2 생성방식과 유입경로

생성 방식은 다음 세 가지로 구분한다. 메일·슬랙·회의·AI 채팅은 업무 내용을 가져오거나 생성 명령을 전달하는 경로이며, 그 자체가 별도의 수락 정책을 뜻하지 않는다.

| 생성 방식 | 의미 | 예시 |
|---|---|---|
| 본인 생성 | 자신이 수행할 일을 등록 | 직접 작성, 메일에서 할 일 추출 후 등록, AI에게 내 업무 생성 요청 |
| 요청에 의한 생성 | 다른 사람에게 수행을 요청할 일을 등록 | 디자인 요청, 초안 검토 요청 |
| 자동 생성 | 사전에 설정한 스케줄·이벤트 규칙으로 등록 | 정기 보고서 작성, 이벤트 발생 시 점검 |

메일·슬랙에서 내용을 추출하거나 AI가 업무 후보를 제안한 것만으로 수행 책임이 확정되지는 않는다. 사람이 등록·요청을 결정하거나, 사전에 설정된 자동화 규칙이 생성하도록 정한 경우 Task로 등록한다. 자동화 규칙의 설정자와 실행 경로도 추적할 수 있어야 한다.

#### 1.2.1 본인이 직접 생성

```text
업무 등록 → Task 생성(open), 본인 배정(active)
         → 시작 → in_progress
```

본인이 수행할 업무를 등록하는 행위에 책임을 맡겠다는 의사가 포함되므로 별도 수락 버튼은 필요하지 않다. 생성 직후 바로 진행 중으로 보지 않고, 시작을 기록할 때 `in_progress`로 바꾼다.

#### 1.2.2 다른 사람에게 요청하여 생성

```text
요청 발송 → Task 생성(open), 대상자 배정(pending)
             ├─ 수락 → 같은 Task(open), 배정(active)
             │          └─ 시작 → 같은 Task(in_progress)
             └─ 거절 → 배정(declined), 거절 시각·사유 기록 → Task(cancelled)
```

1. 요청자는 독립 업무 또는 기존 업무의 하위 Task를 만들어 요청한다.
2. 수신자에게 수락·거절 선택을 제공한다. 수락 전에는 요청 대상자이며 아직 수행 책임을 받아들인 상태는 아니다.
3. 수신자가 수락하면 기존 Task의 담당 배정을 활성화한다. 수신자용 Task를 새로 만들거나 복제하지 않는다.
4. 수신자가 시작 버튼을 누르거나 상태를 진행 중으로 변경하면 수행 시작을 기록한다. 두 조작은 같은 상태 변경 로직을 사용한다.
5. 요청자와 수신자는 동일한 Task의 상태와 결과를 본다. 요청자의 상위 업무 연결은 수락 후에도 유지한다.

요청을 거절하면 담당 배정은 `declined`, 해당 Task는 `cancelled`로 종료한다. Task 행은 삭제하지 않고 거절 시각·사유와 취소 이력을 보존한다. 수행 완료(`done`)와는 구분한다. 재요청 방식은 구현 단계에서 정한다.

#### 1.2.3 시스템 트리거로 생성 및 자동 수락

시스템이 트리거에 따라 만드는 업무는 시스템이 보내는 업무 요청으로 처리한다. **자동 수락 여부는 스케줄·메일 등 각 트리거 포인트 설정에서 결정한다.** 업무 생명주기는 트리거별로 별도 수락 규칙을 만들지 않고 해당 설정을 적용한다.

- 자동 수락 꺼짐: Task 생성 + 담당 배정 수락 대기(`pending`).
- 자동 수락 켜짐: Task 생성 + 담당 배정 활성화(`active`), 자동 수락 시각과 적용한 규칙 기록.
- 두 경우 모두 Task는 `open`으로 생성한다. 실제 시작은 담당자가 별도로 기록한다.

예를 들어 매일 업무 일지 작성은 스케줄 트리거에서 자동 수락하도록 설정할 수 있다. 자동 수락은 실제 시작이나 결과물 승인·완료를 뜻하지 않는다.

메일 연동은 현재 개발 범위에 포함하지 않는다. 추후 스케줄·메일 등 트리거 포인트를 구현할 때 자동 수락 설정을 제공한다. 같은 트리거 실행으로 업무가 중복 생성되지 않도록 하는 처리도 해당 구현에서 다룬다.

### 1.3 요청·수락·시작 시각

| 사건 | 기록할 내용 | 의미 |
|---|---|---|
| 업무 생성 | `created_at` | Task가 등록된 시각 |
| 요청 발송 | `requested_at` | 대상자에게 수행을 요청한 시각 |
| 요청 수락 | `accepted_at` | 수신자가 책임을 받아들인 시각 |
| 요청 거절 | `declined_at`, 거절 사유 | 수신자가 요청을 거절한 시각과 이유 |
| 최초 시작 | `activity_events.occurred_at` | 최초 `open → in_progress` 상태 변경 이벤트의 시각 |

위 이름은 기록할 사실을 설명한다. 최초 시작 시각은 기존 상태 변경 이력에서 확인하며 별도 `started_at` 컬럼을 추가하지 않는다. 나머지 기록의 저장 위치는 1.5절에서 구분한다. 요청하면서 생성하면 생성과 발송 시각이 같을 수 있지만 의미는 구분한다. 재요청·재배정은 각각의 배정 이력에 기록하고, 차단 후 재개로 최초 시작 시각을 덮어쓰지 않는다. 시작 시각은 담당자가 시스템에 시작을 기록한 시점이다.

### 1.4 보고서 발행과 디자인 요청 예시

```text
나: 보고서 발행
└─ 표지 디자인 Task — open / 디자이너 수락 대기

디자이너 수락
└─ 동일한 표지 디자인 Task — open / 배정 active

디자이너 시작
└─ 동일한 표지 디자인 Task — in_progress
   ├─ 레퍼런스 조사 Task
   ├─ 시안 구성 Task
   └─ 초안 검토 Task — 나에게 요청 / 수락 대기
```

디자이너가 초안 검토를 요청할 때도 같은 생성 규칙을 적용한다. 초안 검토 Task의 `parent_task_id`는 표지 디자인 ID이며, 내가 수락해도 이 관계는 바뀌지 않는다. 내 업무 화면에서는 초안 검토를 중심 업무로 열 수 있다.

### 1.5 현재 DB 기준 생성 흐름과 입력값

핵심 업무·담당 배정·수락 정보는 기존 테이블로 표현할 수 있다. 아래 요청 흐름은 합의한 목표 모델이며, 현재 일반 요청의 ‘수락 시 Task 생성’ 코드는 변경이 필요하다.

#### 1.5.1 본인이 직접 수행할 업무

```text
업무 등록
→ tasks 생성: state = open
   parent_task_id = 상위 업무 ID 또는 NULL
→ task_assignments 생성
   assignee_id = 본인, assignment_kind = self
   status = active, accepted_at = 생성 시각
→ 시작 버튼 또는 진행 중으로 변경
   같은 tasks.state = in_progress
```

직접 생성은 별도 수락 절차 없이 본인이 수행 책임을 맡는다. 생성과 실제 시작은 구분한다.

#### 1.5.2 다른 사람에게 요청할 업무

```text
업무 생성·요청 발송
→ tasks 생성: state = open
   parent_task_id = 상위 업무 ID 또는 NULL
→ task_assignments 생성
   assignee_id = 요청 대상, assigned_by = 요청자
   status = pending

수락
→ 같은 배정 행의 status = active, accepted_at 기록
→ 같은 Task는 open 유지

시작
→ 같은 Task의 state = in_progress
→ 최초 시작 시각 기록

거절
→ 배정 행의 status = declined
→ declined_at, decline_reason 기록
→ 같은 Task의 state = cancelled
```

요청자와 수신자는 같은 Task를 본다. 수락 시 Task를 추가로 만들지 않으며, 상위 업무 연결도 유지한다. 거절 시 해당 Task는 `cancelled`로 종료하고 행과 이력을 유지한다. 재요청 방식은 구현 단계에서 정한다.

#### 1.5.3 필수값과 선택값

현재 DB 모델과 Task 생성 입력 검증을 기준으로 한다.

| 값 | 필수·선택 | 저장 위치 및 처리 |
|---|---|---|
| 업무명 | 필수, 1~300자 | `tasks.title` |
| 담당자 | 다른 사람에게 요청할 때 필수 | `task_assignments.assignee_id`. 직접 생성은 본인으로 설정 |
| 내용 | 선택 | `tasks.description` |
| 시작일 | 선택 | `tasks.start_date`, 날짜 단위 |
| 기한 | 선택 | `tasks.due_date`, 날짜 단위 |
| 상위 업무 | 독립 업무는 비움, 하위 생성 시 연결 | `tasks.parent_task_id`에 바로 위 Task ID 저장 |
| 프로젝트 | 선택 | `tasks.project_id`. 현재 직접 생성 입력에서 지원하며 담당자 지정 입력에서는 지원하지 않음 |

시작일과 기한을 모두 입력하면 시작일이 기한보다 늦을 수 없다. 기대 결과·완료 기준 전용 컬럼은 현재 없으며, 필요하면 `description`에 작성한다. 이 문서의 의미상 정의를 새로운 DB 필수 컬럼으로 간주하지 않는다.

기존 생성 입력은 체크리스트와 참고 업무도 선택으로 받는다. 각각 `task_checklist_items`, `task_references`에 저장하며, Task 본체 컬럼이나 하위 Task와 혼동하지 않는다.

#### 1.5.4 시스템 기록값과 저장 위치

| 기록 | 저장 위치 |
|---|---|
| 업무 식별자 | `tasks.id` |
| 생성자 | `tasks.created_by_actor_id` |
| 수행 상태 | `tasks.state` |
| 생성 경위·공개 범위 | `tasks.origin_kind`, `visibility` |
| 버전 | `tasks.version` |
| 생성·수정 시각 | `tasks.created_at`, `updated_at` |
| 배정 대상·배정한 사람 | `task_assignments.assignee_id`, `assigned_by` |
| 배정 방식·수락 상태 | `task_assignments.assignment_kind`, `status` |
| 배정 생성 시각 | `task_assignments.created_at` |
| 수락 시각 | `task_assignments.accepted_at` |
| 거절 시각·사유 | `task_assignments.declined_at`, `decline_reason` |

수락·거절 시각은 해당 사건이 발생하기 전에는 비어 있다. 직접 생성의 본인 배정에서는 `assigned_by`를 비우고, `accepted_at`은 생성 시각으로 기록한다.

#### 1.5.5 구현에서 보완할 부분

| 항목 | 현재 구조와 변경 방향 |
|---|---|
| 발송 시 Task 생성 | 담당자 지정 경로는 이미 Task와 대기 배정을 먼저 생성한다. 일반 요청은 수락 시 Task를 생성하므로 목표 흐름에 맞춰 통합·변경해야 한다. 기존 요청 이력은 보존한다. |
| 재귀적인 하위 업무 | `parent_task_id`로 저장 가능하다. 현재 코드의 1단계 제한을 제거하고 조회·권한·완료 검증과 순환 방지를 보완한다. |
| 요청 발송 시각 | 배정 생성과 발송이 같은 사건이면 `task_assignments.created_at`을 사용할 수 있다. 별도 발송이나 재발송은 구분 가능한 이벤트로 기록해야 한다. |
| 최초 실제 시작 시각 | 기존 `activity_events`에서 최초 `open → in_progress` 이벤트의 `occurred_at`을 확인한다. 별도 `started_at` 컬럼은 추가하지 않는다. `tasks.start_date`는 날짜 값으로 구분한다. |
| 자동 수락 | `status = active`와 `accepted_at`으로 수락 결과를 표현할 수 있다. 트리거 포인트의 자동 수락 설정과 적용 규칙 기록은 해당 기능 구현 시 추가한다. |

Task·상위 관계·담당자·수락·거절은 기존 테이블을 활용한다. 새로운 업무 본체 테이블을 만들기보다 생성 경로를 통일하고, 시간 기록과 자동화 정보를 보완한다.

## 2. 수락·거절

### 2.1 적용 대상

수락은 요청받은 업무의 수행 책임을 받아들이는 행위다. 본인이 직접 만든 업무는 생성하면서 책임을 맡으므로 별도 수락 절차가 없다. 사람이나 시스템이 보낸 요청은 수락·거절할 수 있으며, 시스템 요청은 트리거 포인트 설정에 따라 자동 수락할 수 있다.

### 2.2 수락 상태와 진행 상태의 구분

**수락 상태는 담당 배정의 상태이고, 진행 상태는 업무 자체의 상태다.** 수락은 “이 일을 맡겠다”는 뜻이며, 실제 수행을 시작했다는 뜻은 아니다.

| 구분 | 의미 | 저장 위치 |
|---|---|---|
| 수락 상태 | 담당자가 이 일을 맡았는가? | `task_assignments.status` |
| 진행 상태 | 업무가 어디까지 진행됐는가? | `tasks.state` |

| 상황 | 담당 배정 상태 | 업무 진행 상태 |
|---|---|---|
| 수락 대기 | `pending` | `open` |
| 수락했지만 시작 전 | `active` | `open` |
| 수락 후 수행 중 | `active` | `in_progress` |

수락하면 같은 배정 행의 상태를 `active`로 바꾸고 `accepted_at`을 기록한다. Task를 추가로 생성하지 않으며, 업무 상태는 `open`을 유지한다. 담당자가 시작 버튼을 누르거나 진행 중으로 변경할 때 `in_progress`로 전환한다.

거절하면 배정 상태를 `declined`로 바꾸고 `declined_at`과 거절 사유를 기록한다. 동시에 해당 Task를 `cancelled`로 변경해 요청 업무를 종료한다. Task 행은 남기며, 수행 완료인 `done`으로 처리하지 않는다. 상위 업무를 자동으로 취소하거나 완료하지 않는다.

### 2.3 기존 work_requests 처리

합의한 기본 흐름은 `tasks`와 `task_assignments`를 중심으로 구현한다. 기존 `work_requests`의 유지·통합·전환 여부와 요청 협의·검토 이력 및 기존 데이터의 처리 방식은 구현 단계에서 결정한다. 이 문서에서는 테이블 삭제나 기존 기능 제거를 확정하지 않는다.

## 3. 시작

### 3.1 시작의 의미와 조건

시작은 담당자가 업무 수행에 착수했음을 기록하는 행위다. 본인이 직접 생성했거나 요청을 수락하여 담당 배정이 `active`인 업무에서 시작할 수 있다. 수락 대기 또는 거절된 배정으로는 업무를 시작할 수 없다.

수락은 수행 책임을 맡는 결정이며, 시작과 구분한다. 수락만으로 업무를 진행 중으로 바꾸지 않는다.

### 3.2 상태 변경

```text
시작 전: tasks.state = open / task_assignments.status = active
→ 시작 버튼 또는 진행 중으로 상태 변경
→ tasks.state = in_progress
```

시작 버튼과 진행 중으로의 상태 변경은 같은 시작 전환으로 처리한다. 담당 배정은 `active`를 유지한다. 현재 코드의 상태 전환·담당자 검증·버전 검사 로직을 활용한다.

### 3.3 시작 시각과 이력

**`open → in_progress`로 바뀐 시각이 시작 시각이다.** 기존 상태 변경 이력에 날짜와 시간이 기록되므로 별도 시작 시각 컬럼을 추가하지 않는다.

| 기록 | 저장 위치와 의미 |
|---|---|
| 시작 시각 | `activity_events.occurred_at`: `open → in_progress` 상태 변경 시각 |
| 시작한 사람 | `activity_events.actor_id` |
| 상태 변경 내용 | `activity_events.event_kind = task.state_changed`, 변경 전 참조·변경 후 참조 및 요약 |
| 시작일 | `tasks.start_date`: 비어 있으면 시작 시 오늘 날짜로 설정하고, 기존 값이 있으면 유지 |

시작일은 날짜 값이며, 시작 이벤트의 정확한 시각과 구분한다. 최초 시작은 최초 `open → in_progress` 이벤트로 확인한다. 차단 후 재개나 완료 후 재개는 별도의 상태 변경 이력으로 남기고 최초 시작 기록을 덮어쓰지 않는다.

## 4. 진행 중

### 4.1 수행 중 가능한 작업

담당자는 진행 중인 업무에서 다음 작업을 할 수 있다. 각 작업의 권한과 변경 이력을 관리하며, 내용을 수정하거나 항목을 추가하는 것만으로 업무 상태를 바꾸지는 않는다.

- 업무명·내용·시작일·기한 수정
- 체크리스트 작성·수정·체크·체크 해제·순서 변경·보관 처리
- 파일·링크·참고 자료 추가 및 연결 해제
- 참고 업무 연결 및 해제
- 직접 수행할 하위 Task 생성
- 다른 사람에게 하위 Task 요청
- 업무 변경 이력과 버전 확인
- 권한이 있는 사람의 담당자 재배정

프로젝트 연결도 변경할 수 있으나 하위 Task는 상위 업무의 프로젝트를 따른다. 댓글은 현재 일반 업무 요청의 대화에서 지원하며, 모든 Task에 직접 다는 기능은 아직 공통으로 구현되어 있지 않다.

### 4.2 체크리스트의 정의

**체크리스트는 업무의 완료 여부를 판단하는 확인 항목이다. 하위 업무는 그 완료 기준을 충족하기 위해 분리하여 수행하는 Task다.**

```text
업무: 로그인 기능 구현

체크리스트 — 완료 기준
□ 올바른 계정으로 로그인된다
□ 잘못된 비밀번호에 오류가 표시된다
□ 로그인 후 세션이 유지된다

하위 업무 — 수행할 일
├─ 백엔드 로그인 API 개발
└─ 프론트엔드 로그인 화면 개발
```

체크리스트는 업무 생성 단계부터 작성할 수 있고, 진행 중에도 추가·수정할 수 있다. 하위 업무를 끝냈더라도 실제 결과를 확인하기 전에는 체크리스트가 충족된 것으로 보지 않는다.

모든 활성 체크리스트 항목을 확인해야 업무를 완료할 수 있다. 보관한 항목은 완료 검사 대상에서 제외한다. 모두 체크해도 업무를 자동 완료하지 않으며, 담당자의 완료 처리 또는 필요한 결과 확인 절차를 거친다. 체크리스트가 없는 업무의 완료 기준 입력 의무는 별도로 정한다.

현재 코드는 체크리스트 생성·수정을 지원하지만, 미완료 체크리스트가 있으면 업무 완료를 막는 검증은 추가해야 한다.

### 4.3 하위 업무를 만드는 범위

담당자는 자신이 관리하는 중심 업무 아래 한 단계로 작업을 구성한다. 같은 담당자가 직접 수행할 작업을 하위의 하위로 계속 분해하는 것은 허용하지 않는다.

```text
내 업무: 기능 구현
├─ 백엔드 API 개발 — 내가 수행
└─ 레포지토리 설계 — 내가 수행
```

백엔드 API 개발을 다른 담당자에게 요청하고 그 사람이 수락했다면, 수신자는 해당 Task를 자신의 중심 업무로 삼아 그 아래에 작업을 만들 수 있다. 전체 데이터는 담당자 간 요청을 따라 여러 단계가 될 수 있지만, 각 담당자가 자신의 작업을 구성하는 범위는 한 단계다.

이 규칙은 기본 화면 표시뿐 아니라 생성 시에도 검증한다. 현재 코드의 전체 1단계 제한을 단순히 없애는 대신, 수락한 요청 업무를 중심으로 하위 작업을 만들 수 있도록 변경해야 한다.

### 4.4 진행 상태가 바뀌는 경우

| 상황 | 상태 변경 |
|---|---|
| 진행이 막힘 | `in_progress → blocked`, 차단 사유 기록 |
| 확인이 필요한 업무의 완료 보고 제출 | `in_progress → completion_submitted` |
| 확인 없이 완료할 수 있는 업무의 완료 처리 | `in_progress → done` |
| 업무 취소 | `in_progress → cancelled` |

상태 변경의 세부 조건은 각 단계에서 정리한다. 검토를 하위 Task로 요청했다는 사실만으로 업무 본체의 상태를 자동 변경하지 않는다.

## 5. 막힘

진행할 수 없는 사유가 생기면 `in_progress → blocked`로 변경한다. **막힘 사유는 필수**이며, 공백만 입력할 수 없다.

막힘 이후에는 다음 상태로 전환할 수 있다.

- 재개: `blocked → in_progress`
- 취소: `blocked → cancelled`

막힘에서 바로 완료할 수는 없다. 진행 중으로 재개한 뒤 완료 처리한다. 재개하면 현재 `block_reason`은 비워지지만, 기존 막힘 사유와 상태 변경 시각은 활동 이력에 남는다.
